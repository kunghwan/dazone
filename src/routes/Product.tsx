import React from "react";

interface ProductProps {
  imgs: string[]; // 이미지 배열
  name: string; // 제품 이름
  price: string; // 가격
  quan: number; // 수량
}

const Product = () => {
  return <></>;
};

export default Product;
